{
  "scenario": "Reachable via an aliased whole-module import.",
  "sink": {"file": "repo/pkg/sinkmod.py", "line": 1},
  "entrypoints": [{"file": "repo/pkg/entry.py", "line": 3}],
  "allowed_verdicts": ["reachable"],
  "forbidden_verdicts": ["not_reachable", "unknown", "reachable_only_from_tests"],
  "decidable": true,
  "human_note": "entry.py imports the whole sinkmod module under the alias sm and calls sm.vulnerable() when run as a script. Renaming the module on import does not change which function is called."
}
