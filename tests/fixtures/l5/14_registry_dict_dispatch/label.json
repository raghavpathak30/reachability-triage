{
  "scenario": "Call through a registry dict populated with a named function at import time.",
  "sink": {"file": "repo/pkg/sink.py", "line": 1},
  "entrypoints": [{"file": "repo/pkg/entry.py", "line": 4}],
  "allowed_verdicts": ["unknown", "reachable"],
  "forbidden_verdicts": ["not_reachable"],
  "decidable": false,
  "human_note": "vulnerable is stored, by name, as a value in the HANDLERS dict at import time. entry.py looks up a key in that dict and calls whatever function it finds; the one call site passes the key \"go\", which does map to vulnerable, but the lookup key comes from a function argument, so other keys cannot be ruled out."
}
